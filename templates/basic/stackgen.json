{
  "project_name": "{{ project_name }}",
  "description": "{{ description }}",
  "version": "{{ version }}",
  "template": "{{ template }}",

  "options": {
    "template_source": {
      "value": null,
      "default": "embed:basic",
      "required": true
    },
    "project_name": {
      "value": null,
      "default": "my_project",
      "required": true
    },
    "project_description": {
      "value": null,
      "default": "Basic Project",
      "required": true
    },
    "project_version": {
      "value": null,
      "default": "v0.1.0",
      "required": true
    }
  }
}